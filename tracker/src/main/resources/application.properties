# Set the port to match your previous work
server.port=8081

# MySQL Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/finance_tracker?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Prajeen@2005

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# Use the updated dialect to avoid warnings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Allow access to static HTML files
spring.web.resources.static-locations=classpath:/static/